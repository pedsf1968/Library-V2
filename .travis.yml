language: java
sudo: true
dist: trusty

# use Java 8
jdk: openjdk8

before_install:
  - chmod +x mvnw

jobs:
  include:
    - stage: test
      script: ./mvnw clean test -q -P test
    - stage: sonar
      script: ./mvnw verify sonar:sonar -q -P test

cache:
  directories:
    - $HOME/.m2

notifications:
  email: pe.dsf@free.fr
